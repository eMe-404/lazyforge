% shell, navigation

# [z] Jump to a frecent directory
z <directory>

# [z] Show zoxide scored history
zoxide query --list --score

# [Ctrl+T] Open fzf file picker
fzf

# [fif] Fuzzy search file contents with bat preview
rg --files --hidden --glob '!.git' | fzf --preview "bat --color=always {}"

# [Ctrl+R] Search shell history interactively
atuin search

# [Ctrl+R] Search history for a specific term
atuin search <term>

$ directory: zoxide query --list | awk '{print $2}'


% files, eza, bat

# [ll] List files with icons and git status
eza -la --icons --git

# [ll] List as a tree (N levels deep)
eza --tree --level <depth> --icons

# [cat] Preview a file with syntax highlighting
bat <file>

# [cat] Preview a file forcing a specific language
bat --language <lang> <file>

# [cat] Preview a line range of a file
bat -r <start>:<end> <file>

# [cat] Show all languages bat supports
bat --list-languages

$ depth: echo -e "2\n3\n4\n5"
$ lang: bat --list-languages | awk -F: '{print $1}'


% search, ripgrep, fd

# [rg] Search for a pattern in files
rg <pattern>

# [rg] Search in a specific file type only
rg <pattern> --type <filetype>

# [rg] Search with N lines of context
rg -C <lines> <pattern>

# [rg] List only files that contain the pattern
rg -l <pattern>

# [rg] Case-insensitive search
rg -i <pattern>

# [fd] Find files by name
fd <filename>

# [fd] Find files by extension
fd --extension <ext>

# [fd] Find directories only
fd --type d <name>

# [bat] Show all file types bat supports
bat --list-languages

$ filetype: echo -e "js\nts\npy\ngo\nrust\njava\nmd\njson\nyaml\nlua"
$ lines: echo -e "2\n5\n10"


% git, lazygit, delta, gh, tig

# [lg] Open lazygit TUI (full git workflow)
lazygit

# [tig] Browse git log read-only
tig

# [delta] View git diff with syntax highlighting
git diff

# [delta] View staged diff
git diff --staged

# [gh] List open pull requests
gh pr list

# [gh] Create a new PR interactively
gh pr create

# [gh] View current PR in browser
gh pr view --web

# [gh] Check CI run status
gh run list

# [gh] Watch a CI run live
gh run watch

# [gh] View open issues
gh issue list

# [gh] Clone a repo
gh repo clone <repo>

# [gh] Create a new GitHub repo
gh repo create <name> --public

$ repo: gh repo list --limit 20 --json nameWithOwner --jq '.[].nameWithOwner'


% json, yaml, data, jq, yq, fx

# [jq] Pretty-print JSON file
jq '.' <file>

# [jq] Extract a top-level field
jq '.<field>' <file>

# [jq] Extract nested field
jq '.data.<field>' <file>

# [jq] Filter array elements by field value
jq '.[] | select(.<field> == "<value>")' <file>

# [jq] Get array length
jq '.[] | length' <file>

# [jq] Pretty-print JSON from curl
curl -s <url> | jq '.'

# [fx] Interactively explore a JSON file
fx <file>

# [fx] Pipe curl response into fx explorer
curl -s <url> | fx

# [yq] Pretty-print a YAML file
yq '.' <file>

# [yq] Extract a YAML field
yq '.<key>' <file>

# [yq] Convert YAML to JSON
yq -o=json '.' <file>

# [yq] Convert JSON to YAML
yq -P '.' <file>


% http, api, xh

# [xh] Simple GET request
xh GET <url>

# [xh] POST JSON key-value pairs
xh POST <url> <key>=<value>

# [xh] POST raw JSON body
xh POST <url> Content-Type:application/json --raw '<json>'

# [xh] Add Authorization Bearer header
xh GET <url> Authorization:"Bearer <token>"

# [xh] Download file to disk
xh GET <url> -o <output>

# [xh] Verbose request showing headers
xh -v GET <url>

# [xh] Add query params
xh GET <url> key==<value>


% system, monitoring, btop, dust, duf, gping

# [btop] Resource monitor — CPU, RAM, network
btop

# [dust] Visual disk usage tree
dust

# [dust] Disk usage for a specific directory
dust <directory>

# [duf] Disk free — all mount points
duf

# [gping] Ping with live graph
gping <host>

# [gping] Ping multiple hosts side by side
gping <host1> <host2>

$ host: echo -e "google.com\n1.1.1.1\n8.8.8.8"
$ host1: echo -e "google.com\n1.1.1.1"
$ host2: echo -e "cloudflare.com\n8.8.8.8"


% docker, lazydocker

# [ld] Open lazydocker TUI
lazydocker

# [docker] View running containers
docker ps

# [docker] Tail container logs
docker logs -f <container>

$ container: docker ps --format '{{.Names}}'


% files, yazi, broot

# [yazi] Open yazi TUI file manager
yazi

# [yazi] Open yazi in a specific directory
yazi <directory>

# [broot] Open broot fuzzy tree navigator
broot

# [broot] Open broot in a specific directory
broot <directory>


% zellij, multiplexer, sessions

# [zellij] Start a new session
zellij

# [zellij] Start a named session
zellij -s <session_name>

# [zellij] List all sessions
zellij list-sessions

# [zellij] Attach to an existing session
zellij attach <session_name>

# [zellij] Kill a session
zellij kill-session <session_name>

# [zellij] Detach — keep session running (Ctrl+o then d)
# Press: Ctrl+o → d


% just, task-runner

# [just] List all available recipes
just --list

# [just] Run a recipe
just <recipe>

# [just] Run a recipe with a variable
just <recipe> <var>=<value>

# [just] Dry-run — show commands without executing
just --dry-run <recipe>

$ recipe: just --list --unsorted 2>/dev/null | tail -n +2 | awk '{print $1}'


% docs, tldr, glow, navi

# [tldr] Look up practical command examples
tldr <command>

# [tldr] Update local tldr cache
tldr --update

# [glow] Render a Markdown file in terminal
glow <file>

# [glow] Browse all Markdown files in current directory
glow .

# [navi] Open cheatsheet browser
navi

# [? / cheat] Search navi for a topic
navi --query <topic>


% editor, neovim, lazyvim

# [nv] Open neovim in current directory
nvim .

# [nv] Open a specific file in neovim
nvim <file>

# [nv →  Space ff] Find files with Telescope (inside nvim)
# Inside nvim: press <Space>ff

# [nv →  Space gg] Open lazygit inside nvim
# Inside nvim: press <Space>gg

# [nv →  Space e] Toggle file tree (inside nvim)
# Inside nvim: press <Space>e

# [nv →  :Lazy] Open plugin manager UI
# Inside nvim: :Lazy

# [nv →  :LazyHealth] Run health check
# Inside nvim: :LazyHealth


% claude, ai

# [cc] Start new Claude Code session
claude

# [ccc] Continue last Claude session
claude --continue

# [cc] Ask Claude a quick inline question
claude "<question>"

# [cc] Claude with a file as context
claude --file <file> "<prompt>"


% git, opencommit, ai-commits

# [gai] Generate AI commit message from staged diff
oco

# [gai] Generate and auto-commit without prompt
oco --yes

# [gai] Set up oco as a git commit hook
oco hook set

# [gai] Remove oco git hook
oco hook unset


% repos, onefetch, tokei

# [repo] Show git repo summary (languages, contributors, stats)
onefetch

# [repo] Repo summary for a specific directory
onefetch <directory>

# [tokei] Count lines of code by language
tokei

# [tokei] Stats for a subdirectory
tokei <directory>

# [tokei] Sort by lines of code
tokei --sort code

$ directory: fd --type d --max-depth 2 | fzf


% system, monitoring, viddy

# [viddy] Watch a command with diff highlighting
viddy '<command>'

# [viddy] Watch docker container state
viddy 'docker ps'

# [viddy] Poll CI run status every 2s
viddy -n 2 'gh run list --limit 5'

# [viddy] Watch kubectl pods
viddy -d 'kubectl get pods'

$ command: echo -e "docker ps\ndf -h\ngit status"


% scripts, gum, interactive

# [gum] Confirm before a destructive action
gum confirm "<message>"

# [gum] Pick from a list interactively
gum choose <option1> <option2> <option3>

# [gum] Styled log output in a script
gum style --foreground 212 "<message>"

# [gum] Spinner around a slow command
gum spin --spinner dot --title "<title>" -- <command>

# [gum] Prompt for user input
gum input --placeholder "<hint>"

$ option1: echo -e "yes\nno\nmaybe"
$ option2: echo -e "a\nb\nc"
$ option3: echo -e "x\ny\nz"


% mise, runtime, versions

# [mise] Install a runtime version
mise install <runtime>@<version>

# [mise] Set runtime version for current project
mise use <runtime>@<version>

# [mise] Set global default runtime version
mise use --global <runtime>@<version>

# [mise] List all installed runtimes
mise list

# [mise] List available versions for a runtime
mise ls-remote <runtime>

# [mise] Show current active versions
mise current

# [mise] Run a command with a specific runtime version
mise exec <runtime>@<version> -- <command>

$ runtime: echo -e "node\npython\ngo\nruby\njava\nrust"
$ version: mise ls-remote node 2>/dev/null | tail -20 | awk '{print $1}'


% logs, tailspin, log-viewer

# [logs] View a log file with syntax highlighting
tspin <file>

# [logs] Follow a log file (like tail -f)
tspin --follow <file>

# [logs] Pipe command output through tailspin
<command> | tspin

# [logs] Highlight docker container logs
docker logs -f <container> | tspin

$ file: fd --type f --extension log | fzf
$ container: docker ps --format '{{.Names}}'
$ command: echo -e "cat app.log\ntail -f /var/log/system.log"


% diff, difftastic, structural-diff

# [dt] Structural diff between two files
difft <file1> <file2>

# [dt] Diff using git difftool (last commit)
git difftool HEAD~1

# [dt] Diff staged changes
git difftool --staged

# [dt] Diff two commits
git difftool <commit1> <commit2>

# [dt] Diff a specific file between commits
git difftool <commit1> <commit2> -- <file>

$ file1: fd --type f | fzf
$ file2: fd --type f | fzf
$ commit1: git log --oneline | fzf | awk '{print $1}'
$ commit2: git log --oneline | fzf | awk '{print $1}'


% claude, ccrider, sessions

# [cr] Browse and resume Claude Code sessions
ccrider

# [cr] Search sessions by keyword
ccrider search "<query>"

# [cr] List recent sessions
ccrider list

$ query: echo -e "bug fix\nrefactor\nfeature\nreview"


% vhs, terminal-recording

# [vhs] Create a new tape file (recording script)
vhs new <name>.tape

# [vhs] Record a tape to GIF
vhs <name>.tape

# [vhs] Record to a specific output format
vhs <name>.tape --output <output>

# [vhs] Validate a tape file without recording
vhs validate <name>.tape

$ name: echo -e "demo\nsetup\nworkflow\ninstall"
$ output: echo -e "demo.gif\ndemo.mp4\ndemo.webm"


% yaer-forge, forge

# [forge] Run full install on a fresh machine
./install.sh

# [forge] Install all brew tools from Brewfile
brew bundle install

# [forge] Search this cheatsheet
navi --query yaer-forge
